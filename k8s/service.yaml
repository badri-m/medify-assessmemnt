apiVersion: v1
kind: Service
metadata:
  name: devops-assessment-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: devops-assessment
  ports:
    - port: 3000
      targetPort: 3000
